/*const {const args = message.content.slice(config.prefix.length).trim().split(" ");
    const cmd = args.shift()?.toLowerCase();} = require("discord.js");

module.exports = class extends Client{
    constructor
}*/